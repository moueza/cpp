#include <iostream>

using namespace std;
class Voiture
{
public:
  int i;

  //Voiture::Voiture();
  Voiture();

  // Voiture::Voiture(int n);
  Voiture(int n);

  //Voiture::~Voiture();
  ~Voiture();

  void affiche();
};
